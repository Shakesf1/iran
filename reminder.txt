The "Fix-and-Push" Workflow:

git pull --rebase (Get the latest .db from the server).

Make your code changes.

git add .

git commit -m "Update logic"

git push



==========

source .venv/bin/activate