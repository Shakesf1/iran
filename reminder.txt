git add index.html shipping.py

# 2. Pull the latest DB from the bot and put your changes on top
git pull --rebase --autostash origin main

# 3. Now push
git commit -m "Quick fix"
git push origin main


==========

source .venv/bin/activate